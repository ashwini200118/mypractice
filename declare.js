// declare.js
function showVariables() {
    var name = "Ashwini";      // var - function scoped
    let age = 23;              // let - block scoped
    const city = "Mysuru";     // const - constant value

    console.log("Name: " + name + "\nAge: " + age + "\nCity: " + city);
}
